const pen = {brand: 'econo', price: 10, color: 'black'}
const pencil = new Object() //create or add new properties
console.log(pencil)
const rubber = Object.create({})   //create or add new properties
console.log(rubber)