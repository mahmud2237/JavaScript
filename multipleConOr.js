const salary = 25000;
const isBCS = true;
const height = 69;

/**
 * Or condition
 * You don't have to fullfilled the all conditions. If any single of them are fullfill the requarement than it will run.
 */
if (salary > 20000 || height < 50){ 
    console.log('Very good')

}
else{
    console.log('Bad luck')
}

